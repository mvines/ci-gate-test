steps:
  - command: "sleep 60"
    label: "sleep [public]"
  - wait
  - command: "set; touch foo; ls -l foo"
    label: "hi [public]"
    plugins:
      docker#v1.1.1:
        image: "rust"
        user: "998:997"
        environment:
          - FOO

       
