steps:
  - command: "echo $CI; whoami; id; apt-get update; apt-get install -y wget"
    label: "fake step [public]"
    plugins:
      docker#v1.1.1:
        image: "blang/latex:ubuntu"
        environment:
          - CI=$CI
