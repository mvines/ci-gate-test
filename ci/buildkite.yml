steps:
  - command: "echo $CI; whoami; id; if ! which wget; then apt-get update; apt-get install -y wget; fi; wget --help"
    label: "fake step [public]"
    plugins:
      docker#v1.1.1:
        image: "blang/latex:ubuntu"
        environment:
          - CI=$CI
